---
pagination:
  data: projects
  size: 1
  alias: project
permalink: "projects/{{ project.slug }}/"
layout: "base.njk"
eleventyComputed:
  title: "{{ project.title }}"
---

<section class="container mt-1">

  {% if project.imageUrl %}
    <img src="{{ project.imageUrl }}?w=1200&auto=format" alt="{{ project.title }}" style="width:100%; height:auto; border-radius: var(--box-radius); margin-bottom: 1.5rem;" />
  {% endif %}

  <h1>{{ project.title }}</h1>

  <div class="project-content">
    {{ project.contentHtml | safe }}
  </div>

  <p><a href="/projects/">← Back to projects</a></p>

</section>
